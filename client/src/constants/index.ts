export const colors = ['red', 'green', 'yellow', 'blue', 'black', 'pink']

export enum Paths {
    Root = '/',
    Login = '/',
    Rooms = '/rooms',
    Art = '/art',
}

export const serverPort = 8099
